<div class="row">
<%= form_for(@topic) do |f| %>
  <%= f.text_area :content, class: 'form-control', placeholder: '今なにしてる？' %>
  <%= f.submit '投稿', class: 'btn btn-primary btn-sm new-post' %>
<% end %>
</div>
