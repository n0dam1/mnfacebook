<%= form_for([topic, comment], remote: true) do |f| %>
<%= f.hidden_field :topic_id %>
<div class="field">
  <%= f.text_area :content, placeholder: "コメントする…", class: "form-control" %>
</div>
<div class="actions">
  <%= f.submit "コメントする", class: "btn btn-primary btn-block" %>
</div>
<% end %>
